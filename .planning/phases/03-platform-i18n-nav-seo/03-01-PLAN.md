---
phase: 03-platform-i18n-nav-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/tr.json
  - src/messages/en.json
  - src/__tests__/i18n-keys.test.ts
  - src/app/(main)/[locale]/galeri/page.tsx
  - src/app/(main)/[locale]/urun/[slug]/page.tsx
  - src/app/(main)/[locale]/page.tsx
  - src/components/gallery/artwork-card.tsx
  - src/components/gallery/artwork-grid.tsx
  - src/components/gallery/category-filter.tsx
  - src/components/gallery/contact-form.tsx
  - src/components/gallery/whatsapp-button.tsx
autonomous: true
requirements:
  - PLT-02

must_haves:
  truths:
    - "TR and EN JSON files have identical key structures"
    - "No inline locale === 'tr' ? ternaries remain in any component for UI label strings"
    - "CategoryFilter 'All' button renders translated text from t('gallery.filterAll')"
    - "ContactForm labels, placeholders, and messages render from translation keys"
    - "WhatsApp button text comes from translation key"
    - "Artwork detail page labels (Artist, Medium, Dimensions, Year, Price, Sold) come from translation keys"
  artifacts:
    - path: "src/messages/tr.json"
      provides: "All Turkish translation strings"
      contains: "gallery"
    - path: "src/messages/en.json"
      provides: "All English translation strings"
      contains: "gallery"
    - path: "src/__tests__/i18n-keys.test.ts"
      provides: "Key parity test between TR and EN"
  key_links:
    - from: "src/components/gallery/contact-form.tsx"
      to: "src/messages/tr.json"
      via: "useTranslations('contact')"
      pattern: "useTranslations\\('contact'\\)"
    - from: "src/components/gallery/category-filter.tsx"
      to: "src/messages/tr.json"
      via: "useTranslations('gallery')"
      pattern: "useTranslations\\('gallery'\\)"
    - from: "src/app/(main)/[locale]/urun/[slug]/page.tsx"
      to: "src/messages/tr.json"
      via: "getTranslations({ locale, namespace: 'gallery' })"
      pattern: "getTranslations.*gallery"
---

<objective>
Expand translation JSON files with all UI strings and replace every inline locale ternary with proper next-intl `useTranslations` / `getTranslations` calls across all Phase 2 components and pages.

Purpose: PLT-02 requires all pages to be viewable in both Turkish and English. Currently, most UI strings are hardcoded inline ternaries (`locale === 'tr' ? 'X' : 'Y'`). This plan converts them all to proper translation keys so the i18n system works correctly.

Output: Complete tr.json and en.json with all namespaces, all components using `t('key')` pattern, and a key parity test.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-i18n-nav-seo/03-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase that executor needs -->

From src/lib/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['tr', 'en'],
  defaultLocale: 'tr',
})
```

From src/app/(main)/[locale]/layout.tsx:
```typescript
// NextIntlClientProvider already wraps children with all messages
const messages = await getMessages()
<NextIntlClientProvider messages={messages}>
```

From src/app/(main)/[locale]/page.tsx (reference pattern for Client Component):
```typescript
import { useTranslations } from 'next-intl'
const t = useTranslations('common')
t('siteTitle')
```

From src/app/(main)/[locale]/urun/[slug]/page.tsx (reference pattern for Server Component):
```typescript
// Server Components use getTranslations (async)
import { getTranslations } from 'next-intl/server'
// Note: locale === 'tr' ternaries for DB DATA (title, description, medium, dimensions, alt text)
// are CORRECT and must be kept — DB data is not a translation key.
// Only UI LABEL strings (e.g., 'Artist', 'Medium', 'Price', 'Sold') should use t() calls.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand translation JSON files and create key parity test</name>
  <files>src/messages/tr.json, src/messages/en.json, src/__tests__/i18n-keys.test.ts</files>
  <action>
1. Expand `src/messages/tr.json` with all namespaces. The complete structure must be:

```json
{
  "common": {
    "siteTitle": "U-Art Tasarım",
    "loading": "Yükleniyor..."
  },
  "nav": {
    "siteTitle": "U-Art Tasarım",
    "gallery": "Galeri",
    "artists": "Sanatçılar",
    "backToMain": "Ana Sayfa"
  },
  "gallery": {
    "title": "Galeri",
    "filterAll": "Tümünü Göster",
    "emptyState": "Henüz eser eklenmedi",
    "artist": "Sanatçı",
    "medium": "Teknik",
    "dimensions": "Boyut",
    "year": "Yıl",
    "price": "Fiyat",
    "contactForPrice": "Fiyat için iletişime geçin",
    "sold": "Satıldı",
    "contactTitle": "Bilgi Al",
    "whatsappCta": "WhatsApp ile Sor"
  },
  "contact": {
    "nameLabel": "Adınız",
    "namePlaceholder": "Adınızı giriniz",
    "emailLabel": "E-posta",
    "emailPlaceholder": "ornek@eposta.com",
    "messageLabel": "Mesajınız",
    "messagePlaceholder": "Eser hakkında sormak istediklerinizi yazınız...",
    "submit": "Mesaj Gönder",
    "submitting": "Gönderiliyor...",
    "successMessage": "Mesajınız gönderildi. En kısa sürede dönüş yapacağız."
  },
  "meta": {
    "homeTitle": "U-Art Tasarım",
    "homeDesc": "Sade, galeri kalitesinde sanat vitrini.",
    "galleryTitle": "Galeri | U-Art Tasarım",
    "galleryDesc": "Özgün tablolar, heykeller ve baskı resimler."
  }
}
```

2. Create `src/messages/en.json` with identical structure, English values:

```json
{
  "common": {
    "siteTitle": "U-Art Design",
    "loading": "Loading..."
  },
  "nav": {
    "siteTitle": "U-Art Design",
    "gallery": "Gallery",
    "artists": "Artists",
    "backToMain": "Home"
  },
  "gallery": {
    "title": "Gallery",
    "filterAll": "Show All",
    "emptyState": "No artworks yet",
    "artist": "Artist",
    "medium": "Medium",
    "dimensions": "Dimensions",
    "year": "Year",
    "price": "Price",
    "contactForPrice": "Contact for price",
    "sold": "Sold",
    "contactTitle": "Get in Touch",
    "whatsappCta": "Ask via WhatsApp"
  },
  "contact": {
    "nameLabel": "Your Name",
    "namePlaceholder": "Enter your name",
    "emailLabel": "Email",
    "emailPlaceholder": "example@email.com",
    "messageLabel": "Your Message",
    "messagePlaceholder": "Write your questions about the artwork...",
    "submit": "Send Message",
    "submitting": "Sending...",
    "successMessage": "Your message has been sent. We will get back to you shortly."
  },
  "meta": {
    "homeTitle": "U-Art Design",
    "homeDesc": "Simple, gallery-quality art showcase.",
    "galleryTitle": "Gallery | U-Art Design",
    "galleryDesc": "Original paintings, sculptures, and prints."
  }
}
```

3. Create `src/__tests__/i18n-keys.test.ts` — a test that imports both JSON files and recursively compares their key structures. It should:
   - Import both JSON files directly (no mocking needed)
   - Recursively extract all dot-notation keys from each object
   - Assert the key sets are identical (symmetric difference is empty)
   - If keys differ, the error message must list the missing keys in each file

This test is the most valuable in the phase — it catches silent "missing key renders raw key string" bugs at commit time.
  </action>
  <verify>
    <automated>cd /home/cagr/Masaüstü/sanat-galeri && pnpm test -- --testPathPattern=i18n-keys</automated>
  </verify>
  <done>Both JSON files have identical key structures. Test passes confirming key parity. All namespaces (common, nav, gallery, contact, meta) present in both files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all inline locale ternaries with translation calls</name>
  <files>src/app/(main)/[locale]/galeri/page.tsx, src/app/(main)/[locale]/urun/[slug]/page.tsx, src/app/(main)/[locale]/page.tsx, src/components/gallery/artwork-card.tsx, src/components/gallery/artwork-grid.tsx, src/components/gallery/category-filter.tsx, src/components/gallery/contact-form.tsx, src/components/gallery/whatsapp-button.tsx</files>
  <action>
Replace inline locale ternaries with `t()` calls in each file. CRITICAL DISTINCTION: Only replace UI LABEL strings. DB-driven data (title, description, medium text, dimensions text, alt text from DB columns) must STAY as `locale === 'tr' ? product.titleTr : product.titleEn` because those are data, not translations.

**File-by-file changes:**

1. **`src/app/(main)/[locale]/galeri/page.tsx`** (Server Component):
   - Add `import { getTranslations } from 'next-intl/server'`
   - Add `const t = await getTranslations({ locale, namespace: 'gallery' })` in the component body
   - Replace `{locale === 'tr' ? 'Galeri' : 'Gallery'}` with `{t('title')}`

2. **`src/app/(main)/[locale]/urun/[slug]/page.tsx`** (Server Component):
   - Add `import { getTranslations } from 'next-intl/server'`
   - Inside `ArtworkDetailPage`, add `const t = await getTranslations({ locale, namespace: 'gallery' })`
   - Replace these UI labels only:
     - `{isTr ? 'Sanatci' : 'Artist'}` → `{t('artist')}`
     - `{isTr ? 'Teknik' : 'Medium'}` → `{t('medium')}`
     - `{isTr ? 'Boyut' : 'Dimensions'}` → `{t('dimensions')}`
     - `{isTr ? 'Yil' : 'Year'}` → `{t('year')}`
     - `{isTr ? 'Fiyat' : 'Price'}` → `{t('price')}`
     - `{isTr ? 'Satildi' : 'Sold'}` → `{t('sold')}`
     - `{isTr ? 'Bilgi Al' : 'Get in Touch'}` → `{t('contactTitle')}`
     - `isTr ? 'Fiyat icin iletisime gecin' : 'Contact for price'` → `t('contactForPrice')`
   - KEEP all `isTr ? eser.titleTr : eser.titleEn` etc. — those are DB data
   - KEEP `isTr ? img.altTr : img.altEn` — DB data

3. **`src/app/(main)/[locale]/page.tsx`** (Client Component):
   - Currently minimal with just `t('siteTitle')`. Update the placeholder paragraph:
   - Replace `"Galeri sayfasi yapilandirilacak."` with a proper homepage intro. Use `const tNav = useTranslations('nav')` and add a link to gallery: `<Link href={`/${locale}/galeri`}>{tNav('gallery')}</Link>`. The homepage needs `locale` — get it from `useLocale()` from `next-intl`. This is a Client Component already using `useTranslations`.

4. **`src/components/gallery/artwork-card.tsx`** (Server Component — receives locale prop):
   - Replace `{locale === 'tr' ? 'Satıldı' : 'Sold'}` with a translation. Since ArtworkCard is a Server Component that receives `locale` as prop, add `import { getTranslations } from 'next-intl/server'`, make it `async`, add `const t = await getTranslations({ locale, namespace: 'gallery' })`, replace the sold badge: `{t('sold')}`.
   - KEEP `locale === 'tr' ? product.titleTr : product.titleEn` — DB data
   - KEEP the alt text ternary — DB data

5. **`src/components/gallery/artwork-grid.tsx`** (Server Component):
   - Replace `{locale === 'tr' ? 'Henüz eser eklenmedi' : 'No artworks yet'}` with translation.
   - Add `import { getTranslations } from 'next-intl/server'`, make function async, add `const t = await getTranslations({ locale, namespace: 'gallery' })`, use `{t('emptyState')}`.

6. **`src/components/gallery/category-filter.tsx`** (Client Component):
   - Add `import { useTranslations } from 'next-intl'`
   - Add `const t = useTranslations('gallery')` inside component
   - Replace hardcoded `"Tümünü Göster"` with `{t('filterAll')}`

7. **`src/components/gallery/contact-form.tsx`** (Client Component):
   - Add `import { useTranslations } from 'next-intl'`
   - Add `const t = useTranslations('contact')` inside component
   - Replace ALL hardcoded Turkish strings:
     - `"Mesajiniz gonderildi. En kisa surede donus yapacagiz."` → `{t('successMessage')}`
     - `"Adiniz"` label → `{t('nameLabel')}`
     - `"Adinizi giriniz"` placeholder → `{t('namePlaceholder')}`
     - `"E-posta"` label → `{t('emailLabel')}`
     - `"ornek@eposta.com"` placeholder → `{t('emailPlaceholder')}`
     - `"Mesajiniz"` label → `{t('messageLabel')}`
     - `"Eser hakkinda sormak istediklerinizi yaziniz..."` placeholder → `{t('messagePlaceholder')}`
     - `"Gonderiliyor..."` → `{t('submitting')}`
     - `"Mesaj Gonder"` → `{t('submit')}`

8. **`src/components/gallery/whatsapp-button.tsx`** (Server Component):
   - This component does not receive `locale` currently. Two options: (a) add locale prop and use `getTranslations`, or (b) keep it simple since it's only used in artwork detail page which can pass translated text.
   - Approach: Add `locale` prop to WhatsAppButton. Add `import { getTranslations } from 'next-intl/server'`, make async, add `const t = await getTranslations({ locale, namespace: 'gallery' })`, replace `"WhatsApp ile Sor"` with `{t('whatsappCta')}`.
   - Update the call site in `urun/[slug]/page.tsx` to pass `locale={locale}` to WhatsAppButton.
  </action>
  <verify>
    <automated>cd /home/cagr/Masaüstü/sanat-galeri && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>All inline locale ternaries for UI labels replaced with t() calls. Build succeeds. No hardcoded Turkish-only UI strings remain in gallery components (only DB data ternaries kept). ContactForm, CategoryFilter, WhatsAppButton, ArtworkCard, ArtworkGrid, gallery page, artwork detail page all use translation keys.</done>
</task>

</tasks>

<verification>
1. `pnpm test -- --testPathPattern=i18n-keys` — key parity test passes
2. `pnpm build` — full build succeeds with no type errors
3. Grep for remaining inline ternaries: `grep -rn "locale === 'tr' ? '" src/components/gallery/ src/app/\(main\)/` should only show DB data ternaries (titleTr/titleEn, altTr/altEn, etc.), not UI label ternaries
</verification>

<success_criteria>
- Both tr.json and en.json have identical key structures (test proves it)
- All UI label strings in gallery components use t() calls
- DB-driven data strings (title, description, alt) correctly remain as locale ternaries
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-i18n-nav-seo/03-01-SUMMARY.md`
</output>
