---
phase: 03-platform-i18n-nav-seo
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/shared/language-switcher.tsx
  - src/components/shared/navbar.tsx
  - src/app/(main)/[locale]/layout.tsx
  - src/app/(artist)/[locale]/[artist]/layout.tsx
  - src/app/(main)/[locale]/page.tsx
  - src/app/(main)/[locale]/galeri/page.tsx
  - src/app/(artist)/[locale]/[artist]/page.tsx
  - .env.local
  - src/__tests__/navbar.test.ts
  - src/__tests__/language-switcher.test.ts
  - src/__tests__/gallery-metadata.test.ts
autonomous: true
requirements:
  - PLT-04
  - PLT-05

must_haves:
  truths:
    - "Language switcher renders TR and EN links that point to same path with alternate locale"
    - "Navbar shows cross-domain links to main site, melike subdomain, and seref subdomain"
    - "Navbar is visible on every page in both (main) and (artist) layouts"
    - "Homepage has locale-aware generateMetadata with title and description"
    - "Gallery page has locale-aware generateMetadata with title and description"
    - "Artist placeholder page has locale-aware generateMetadata"
    - "Cross-domain links include the current locale segment"
  artifacts:
    - path: "src/components/shared/language-switcher.tsx"
      provides: "TR/EN language toggle component"
      min_lines: 15
    - path: "src/components/shared/navbar.tsx"
      provides: "Shared navbar with cross-domain links and language switcher"
      min_lines: 30
    - path: "src/__tests__/navbar.test.ts"
      provides: "Contract test for navbar cross-domain links"
    - path: "src/__tests__/language-switcher.test.ts"
      provides: "Contract test for language switcher link generation"
    - path: "src/__tests__/gallery-metadata.test.ts"
      provides: "Contract test for locale-aware generateMetadata"
  key_links:
    - from: "src/app/(main)/[locale]/layout.tsx"
      to: "src/components/shared/navbar.tsx"
      via: "import and render"
      pattern: "import Navbar"
    - from: "src/app/(artist)/[locale]/[artist]/layout.tsx"
      to: "src/components/shared/navbar.tsx"
      via: "import and render"
      pattern: "import Navbar"
    - from: "src/components/shared/navbar.tsx"
      to: "src/components/shared/language-switcher.tsx"
      via: "import and render"
      pattern: "import LanguageSwitcher"
    - from: "src/components/shared/navbar.tsx"
      to: "process.env.NEXT_PUBLIC_MAIN_URL"
      via: "env var read"
      pattern: "NEXT_PUBLIC_MAIN_URL"
---

<objective>
Build LanguageSwitcher and Navbar shared components, inject into both layouts, add locale-aware SEO metadata to all pages, and create contract tests.

Purpose: PLT-04 requires consistent navigation across 3 domains. PLT-05 requires per-page SEO title/description. This plan creates the navigation shell and SEO layer.

Output: Working Navbar with language switcher and cross-domain links on every page. Every page has locale-aware `<title>` and meta description.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-i18n-nav-seo/03-RESEARCH.md
@.planning/phases/03-platform-i18n-nav-seo/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — translation keys available after execution -->

nav namespace keys (available in tr.json/en.json):
- nav.siteTitle
- nav.gallery
- nav.artists
- nav.backToMain

meta namespace keys:
- meta.homeTitle / meta.homeDesc
- meta.galleryTitle / meta.galleryDesc

gallery namespace keys:
- gallery.title (and all others from Plan 01)

<!-- Existing patterns -->

From src/middleware.ts:
```typescript
// getTenant reads ?tenant= query param in localhost for subdomain simulation
// Cross-domain links in dev must use ?tenant=melike format
```

From src/lib/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['tr', 'en'],
  defaultLocale: 'tr',
})
```

From src/app/(main)/[locale]/urun/[slug]/page.tsx (reference for generateMetadata):
```typescript
export async function generateMetadata({ params }: PageProps): Promise<Metadata> {
  const { locale, slug } = await params
  // ... DB fetch + locale-aware title/description
}
```

From RESEARCH.md Pattern 1 — LanguageSwitcher:
- Use `useLocale()` from `next-intl` for current locale
- Use `usePathname()` from `next-intl/navigation` for path WITHOUT locale prefix
- Use `Link` from `next/link` with `locale` prop for switching

From RESEARCH.md Pattern 2 — Navbar:
- Server Component using `getTranslations`
- Cross-domain links use absolute URLs from env vars with `<a>` tags (not next-intl Link)
- Cross-domain URLs carry locale segment: `${ARTIST_URL}/${locale}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageSwitcher, Navbar, and inject into both layouts</name>
  <files>src/components/shared/language-switcher.tsx, src/components/shared/navbar.tsx, src/app/(main)/[locale]/layout.tsx, src/app/(artist)/[locale]/[artist]/layout.tsx, .env.local</files>
  <action>
1. **Create `src/components/shared/language-switcher.tsx`** — Client Component:
   - `'use client'` directive
   - Import `useLocale` from `next-intl`, `usePathname` from `next-intl/navigation`, `Link` from `next/link`
   - Read current locale via `useLocale()`, current path (without locale prefix) via `usePathname()`
   - Render two `Link` elements: one with `locale="tr"` and one with `locale="en"`, both with `href={pathname}`
   - Active locale gets `font-semibold` class, inactive gets `text-neutral-500 hover:text-neutral-900`
   - Separated by `|` span with `text-neutral-300`
   - Wrapped in `div` with `flex items-center gap-2 text-sm`

2. **Create `src/components/shared/navbar.tsx`** — Server Component:
   - Import `getTranslations` from `next-intl/server`, import `LanguageSwitcher`
   - Props: `{ locale: string; domain?: 'main' | 'melike' | 'seref' }`
   - Read translations: `const t = await getTranslations({ locale, namespace: 'nav' })`
   - Read cross-domain URLs from env vars with fallbacks:
     - `NEXT_PUBLIC_MAIN_URL` ?? `'https://uarttasarim.com'`
     - `NEXT_PUBLIC_MELIKE_URL` ?? `'https://melike.uarttasarim.com'`
     - `NEXT_PUBLIC_SEREF_URL` ?? `'https://seref.uarttasarim.com'`
   - Render `<header>` with `border-b border-neutral-100`:
     - Left: site title as `<a>` linking to `${MAIN_URL}/${locale}` with text `{t('siteTitle')}`
     - Right: navigation links + LanguageSwitcher in `flex items-center gap-6`:
       - Link to gallery: `<a href="${MAIN_URL}/${locale}/galeri">{t('gallery')}</a>` (use `text-sm text-neutral-600 hover:text-neutral-900`)
       - Artist links: `<a href="${MELIKE_URL}/${locale}">Melike</a>` and `<a href="${SEREF_URL}/${locale}">Şeref</a>` (artist names are proper nouns, not translated)
       - `<LanguageSwitcher />`
   - Wrap nav content in `max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8` for consistent container — BUT since the layout already has this container, the navbar should NOT duplicate it. Use `<nav className="flex items-center justify-between py-4">` only.

3. **Update `src/app/(main)/[locale]/layout.tsx`**:
   - Import `Navbar` from `@/components/shared/navbar`
   - Add `<Navbar locale={locale} domain="main" />` inside the existing `<div>` container, BEFORE `{children}`

4. **Update `src/app/(artist)/[locale]/[artist]/layout.tsx`**:
   - Import `Navbar` from `@/components/shared/navbar`
   - Pass `domain={artist as 'melike' | 'seref'}` to distinguish which subdomain the visitor is on
   - Add `<Navbar locale={locale} domain={artist as 'melike' | 'seref'} />` inside the existing `<div>`, BEFORE `{children}`

5. **Add env vars to `.env.local`** (create or append):
   - `NEXT_PUBLIC_MAIN_URL=http://localhost:3000`
   - `NEXT_PUBLIC_MELIKE_URL=http://localhost:3000?tenant=melike`
   - `NEXT_PUBLIC_SEREF_URL=http://localhost:3000?tenant=seref`

IMPORTANT: The existing container div (`min-w-[320px] mx-auto max-w-screen-xl px-4 sm:px-6 lg:px-8`) in both layouts should NOT be duplicated inside the Navbar. The Navbar renders inside this container.
  </action>
  <verify>
    <automated>cd /home/cagr/Masaüstü/sanat-galeri && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>LanguageSwitcher and Navbar components created. Both layouts render Navbar before children. Cross-domain links use env vars with locale segment. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add locale-aware SEO metadata to all pages and create contract tests</name>
  <files>src/app/(main)/[locale]/page.tsx, src/app/(main)/[locale]/galeri/page.tsx, src/app/(artist)/[locale]/[artist]/page.tsx, src/__tests__/navbar.test.ts, src/__tests__/language-switcher.test.ts, src/__tests__/gallery-metadata.test.ts</files>
  <action>
**SEO Metadata changes:**

1. **`src/app/(main)/[locale]/page.tsx`** — Homepage:
   - This is currently a Client Component (`useTranslations`). To add `generateMetadata`, the metadata export must be in the same file but `generateMetadata` is a Server-only export. Next.js allows both `generateMetadata` (server) and a default client component export in the same file.
   - Add at the top (before the `'use client'` component, or restructure):
     - BETTER APPROACH: Convert homepage to Server Component. Replace `useTranslations` with `getTranslations`. The homepage is simple enough — it just shows a title and a link. Make it `async function HomePage({ params })`, extract locale from params, use `getTranslations`.
   - Add `generateMetadata`:
     ```typescript
     export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
       const { locale } = await params
       const t = await getTranslations({ locale, namespace: 'meta' })
       return { title: t('homeTitle'), description: t('homeDesc') }
     }
     ```

2. **`src/app/(main)/[locale]/galeri/page.tsx`** — Gallery page:
   - Currently has `export function generateMetadata() { return { title: 'Galeri | U-Art Tasarım' } }` — hardcoded.
   - Replace with async version that accepts params and uses translations:
     ```typescript
     export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
       const { locale } = await params
       const t = await getTranslations({ locale, namespace: 'meta' })
       return { title: t('galleryTitle'), description: t('galleryDesc') }
     }
     ```
   - Add `import { getTranslations } from 'next-intl/server'` if not already present (Task 1 of Plan 01 may have added it).
   - Add `import type { Metadata } from 'next'`

3. **`src/app/(artist)/[locale]/[artist]/page.tsx`** — Artist placeholder page:
   - Currently uses `useTranslations` (client hook) in what should be a Server Component (it's async). This is a bug from Phase 1 — `useTranslations` in an async function. Fix it:
     - Remove `import { useTranslations } from 'next-intl'`
     - Add `import { getTranslations } from 'next-intl/server'`
     - Extract `locale` from params: `const { locale, artist } = await params` (update params type to include locale)
     - Use `const t = await getTranslations({ locale, namespace: 'common' })`
   - Add `generateMetadata`:
     ```typescript
     export async function generateMetadata({ params }: { params: Promise<{ locale: string; artist: string }> }): Promise<Metadata> {
       const { locale, artist } = await params
       const name = artist.charAt(0).toUpperCase() + artist.slice(1)
       return { title: `${name} | U-Art`, description: `${name} - artist portfolio` }
     }
     ```

**Contract Tests:**

4. **`src/__tests__/language-switcher.test.ts`** — Contract test (no DOM rendering):
   - Test the logic: given locale 'tr' and pathname '/galeri', the component should produce links to '/galeri' with locale='tr' and locale='en'.
   - Since we cannot render React in unit tests without jsdom, test the URL construction logic:
     - Extract the link-building logic or test that the component file exports/uses the correct imports.
     - Alternative: Write a simple logic test that verifies for each locale, the other locale link is constructed correctly. The key contract: "pathname stays the same, only locale changes".
   - Implementation: Create a helper function `getLanguageLinks(currentLocale: string, pathname: string)` that returns `[{locale: 'tr', href: pathname, active: currentLocale === 'tr'}, {locale: 'en', href: pathname, active: currentLocale === 'en'}]`. Export it from language-switcher.tsx. Test the helper.

5. **`src/__tests__/navbar.test.ts`** — Contract test:
   - Test that cross-domain URL construction is correct.
   - Create and export a helper function `getCrossDomainLinks(locale: string, mainUrl: string, melikeUrl: string, serefUrl: string)` from navbar.tsx that returns the link objects.
   - Test: for locale='tr', links should be `${mainUrl}/tr`, `${melikeUrl}/tr`, `${serefUrl}/tr`.
   - Test: for locale='en', links should use '/en' segment.

6. **`src/__tests__/gallery-metadata.test.ts`** — Contract test:
   - Test that the generateMetadata function from galeri/page.tsx returns locale-aware titles.
   - Mock `next-intl/server` `getTranslations` to return a function that echoes the key.
   - Call `generateMetadata({ params: Promise.resolve({ locale: 'tr' }) })`.
   - Assert the result has `title` and `description` properties (not undefined).
   - This verifies the function signature is correct and it calls getTranslations with the right namespace.
  </action>
  <verify>
    <automated>cd /home/cagr/Masaüstü/sanat-galeri && pnpm test && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>All pages have locale-aware generateMetadata. Contract tests pass for navbar, language-switcher, and gallery-metadata. Build succeeds. Artist page bug (useTranslations in async Server Component) is fixed.</done>
</task>

</tasks>

<verification>
1. `pnpm test` — all tests pass (i18n-keys, navbar, language-switcher, gallery-metadata)
2. `pnpm build` — full build succeeds
3. Every page under `(main)/[locale]/` and `(artist)/[locale]/[artist]/` has a `generateMetadata` export
4. Both layouts import and render Navbar
5. LanguageSwitcher is rendered within Navbar on every page
</verification>

<success_criteria>
- Navbar with language switcher and cross-domain links renders on every page
- Language switcher correctly generates TR/EN toggle links preserving current path
- Cross-domain links include locale segment and use env vars (not hardcoded URLs)
- Every page has unique, locale-aware title and meta description
- All 4 contract tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-i18n-nav-seo/03-02-SUMMARY.md`
</output>
