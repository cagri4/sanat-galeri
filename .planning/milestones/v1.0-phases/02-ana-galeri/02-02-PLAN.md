---
phase: 02-ana-galeri
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/(main)/[locale]/galeri/page.tsx
  - src/components/gallery/artwork-card.tsx
  - src/components/gallery/artwork-grid.tsx
  - src/components/gallery/category-filter.tsx
autonomous: true
requirements: [GAL-01]

must_haves:
  truths:
    - "Gallery page renders artwork cards from DB data in a responsive grid"
    - "Clicking a category filter button updates URL param and grid shows only that category"
    - "Clicking 'Tumunu Goster' (all) clears the filter and shows all artworks"
    - "Gallery page works without JavaScript (URL-based filtering)"
    - "CategoryFilter is wrapped in Suspense to avoid static generation bail-out"
  artifacts:
    - path: "src/app/(main)/[locale]/galeri/page.tsx"
      provides: "Gallery listing page with category filter"
      min_lines: 30
    - path: "src/components/gallery/artwork-card.tsx"
      provides: "Single artwork thumbnail card"
      min_lines: 15
    - path: "src/components/gallery/artwork-grid.tsx"
      provides: "Responsive grid layout for artwork cards"
      min_lines: 10
    - path: "src/components/gallery/category-filter.tsx"
      provides: "Client Component category filter buttons"
      contains: "use client"
  key_links:
    - from: "src/app/(main)/[locale]/galeri/page.tsx"
      to: "src/lib/queries/gallery.ts"
      via: "getProducts(category) and getCategories()"
      pattern: "getProducts|getCategories"
    - from: "src/components/gallery/artwork-card.tsx"
      to: "src/app/(main)/[locale]/urun/[slug]/page.tsx"
      via: "Link href to detail page"
      pattern: "href.*urun/"
    - from: "src/components/gallery/category-filter.tsx"
      to: "URL searchParams"
      via: "router.replace with ?category= param"
      pattern: "category"
---

<objective>
Build the gallery listing page with category-filtered artwork grid. Users browse artworks at /galeri, click category buttons to filter, see responsive card grid with thumbnails.

Purpose: GAL-01 — the primary browsing experience for visitors discovering artworks.
Output: Gallery page, artwork card, artwork grid, category filter components.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ana-galeri/02-RESEARCH.md
@.planning/phases/02-ana-galeri/02-01-SUMMARY.md

@src/app/(main)/[locale]/layout.tsx
@src/lib/i18n/routing.ts
</context>

<interfaces>
<!-- From Plan 01 — gallery query functions the executor will call -->

From src/lib/queries/gallery.ts (created in Plan 01):
```typescript
export async function getProducts(category?: string): Promise<ProductWithImage[]>
export async function getCategories(): Promise<string[]>
```

From src/lib/db/schema.ts (products table — relevant columns for card display):
```typescript
// products: id, slug, titleTr, titleEn, category, price, currency, year, mediumTr, mediumEn, dimensionsTr, dimensionsEn
// productImages: id, url, altTr, altEn, sortOrder
```

From src/app/(main)/[locale]/layout.tsx:
```typescript
// Wraps children in NextIntlClientProvider with max-w-screen-xl container
// Params: { locale: string }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Artwork card and grid components</name>
  <files>src/components/gallery/artwork-card.tsx, src/components/gallery/artwork-grid.tsx</files>
  <action>
1. Create `src/components/gallery/artwork-card.tsx` — a Server Component (no 'use client'):
   - Props: `{ product: ProductWithImage, locale: string }` where ProductWithImage is the return type of getProducts (infer from Drizzle)
   - Renders a `<Link>` wrapping the card, href to `/${locale}/urun/${product.slug}`
   - Uses `next/image` for the thumbnail: `product.images[0]?.url` with fallback placeholder
   - Shows title (titleTr or titleEn based on locale), category badge, price with currency
   - Tailwind classes: aspect-[3/4] for art-appropriate ratio, rounded-lg, shadow-sm, hover:shadow-md transition
   - Image uses `sizes="(max-width: 768px) 50vw, (max-width: 1200px) 33vw, 25vw"`

2. Create `src/components/gallery/artwork-grid.tsx` — a Server Component:
   - Props: `{ products: ProductWithImage[], locale: string }`
   - Renders a CSS Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
   - Maps products to ArtworkCard components
   - If products is empty, shows a centered "Henuz eser eklenmedi" / "No artworks yet" message
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ArtworkCard renders product thumbnail with title/price linking to detail page. ArtworkGrid renders responsive 3-column grid. Both type-check without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Gallery page with category filter</name>
  <files>src/app/(main)/[locale]/galeri/page.tsx, src/components/gallery/category-filter.tsx</files>
  <action>
1. Create `src/components/gallery/category-filter.tsx` — Client Component ('use client'):
   - Props: `{ categories: string[], active: string | null }`
   - Uses `useRouter`, `usePathname`, `useSearchParams` from `next/navigation`
   - Renders a horizontal button bar: one "Tumunu Goster" button + one button per category
   - Active category gets distinct styling (e.g., `bg-neutral-900 text-white` vs `bg-neutral-100 text-neutral-700`)
   - Clicking a category calls `router.replace(pathname + '?category=' + cat)`, clicking "Tumunu Goster" calls `router.replace(pathname)` (removes param)
   - Buttons use `<button>` tags, not `<a>` links — filter is a UI action, not a navigation
   - Wrap in horizontal scroll container for mobile: `flex gap-2 overflow-x-auto pb-2`

2. Create `src/app/(main)/[locale]/galeri/page.tsx` — async Server Component:
   - Imports: `getProducts`, `getCategories` from `@/lib/queries/gallery`
   - Imports: `CategoryFilter` from `@/components/gallery/category-filter`
   - Imports: `ArtworkGrid` from `@/components/gallery/artwork-grid`
   - Props type: `{ params: Promise<{ locale: string }>, searchParams: Promise<{ category?: string }> }`
   - **CRITICAL:** `await searchParams` and `await params` (Next.js 16 async props)
   - Fetches `categories = await getCategories()` and `products = await getProducts(category)`
   - Renders page title "Galeri" as `<h1>`, then `<Suspense fallback={<div className="h-12" />}>` wrapping `<CategoryFilter>`, then `<ArtworkGrid>`
   - **CRITICAL:** CategoryFilter MUST be inside `<Suspense>` because it uses `useSearchParams()`
   - Add basic metadata export: `export function generateMetadata() { return { title: 'Galeri | U-Art Tasarim' } }`
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Gallery page at /[locale]/galeri renders category filter + artwork grid. Category filter updates URL param. Suspense wraps CategoryFilter. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes with no errors
- `pnpm build` succeeds (galeri page is generated)
- Navigate to `http://localhost:3000/tr/galeri` — shows artwork grid with seed data
- Click category button — URL updates with `?category=Tablo`, grid filters
</verification>

<success_criteria>
- Gallery page renders seeded artworks in responsive grid
- Category filter buttons appear with categories from DB
- Clicking a category filters the grid via URL param
- ArtworkCard links to /[locale]/urun/[slug]
- CategoryFilter is wrapped in Suspense
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-ana-galeri/02-02-SUMMARY.md`
</output>
