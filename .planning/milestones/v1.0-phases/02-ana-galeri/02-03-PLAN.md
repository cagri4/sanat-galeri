---
phase: 02-ana-galeri
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/(main)/[locale]/urun/[slug]/page.tsx
  - src/components/gallery/lightbox-viewer.tsx
  - src/components/gallery/next-image-slide.tsx
  - src/components/gallery/contact-form.tsx
  - src/components/gallery/whatsapp-button.tsx
autonomous: false

requirements: [GAL-02, GAL-03, GAL-04, GAL-05]

must_haves:
  truths:
    - "Artwork detail page shows title, medium, dimensions, year, and price"
    - "Clicking an artwork image opens a lightbox with zoom capability"
    - "WhatsApp button opens wa.me link with pre-filled artwork title and page URL"
    - "Contact form validates input and submits successfully to DB"
    - "Contact form shows success message after submission"
    - "Detail page returns 404 for non-existent or hidden artworks"
  artifacts:
    - path: "src/app/(main)/[locale]/urun/[slug]/page.tsx"
      provides: "Artwork detail page"
      min_lines: 50
    - path: "src/components/gallery/lightbox-viewer.tsx"
      provides: "Lightbox with zoom plugin"
      contains: "use client"
    - path: "src/components/gallery/next-image-slide.tsx"
      provides: "Custom next/image render for lightbox"
      contains: "use client"
    - path: "src/components/gallery/contact-form.tsx"
      provides: "Contact form with RHF + zod"
      contains: "use client"
    - path: "src/components/gallery/whatsapp-button.tsx"
      provides: "WhatsApp CTA link"
  key_links:
    - from: "src/app/(main)/[locale]/urun/[slug]/page.tsx"
      to: "src/lib/queries/gallery.ts"
      via: "getProductBySlug(slug)"
      pattern: "getProductBySlug"
    - from: "src/components/gallery/lightbox-viewer.tsx"
      to: "src/components/gallery/next-image-slide.tsx"
      via: "render={{ slide: NextImageSlide }}"
      pattern: "NextImageSlide"
    - from: "src/components/gallery/contact-form.tsx"
      to: "src/lib/actions/contact.ts"
      via: "submitContact Server Action call"
      pattern: "submitContact"
    - from: "src/components/gallery/whatsapp-button.tsx"
      to: "src/lib/utils/whatsapp.ts"
      via: "buildWhatsAppHref()"
      pattern: "buildWhatsAppHref"
---

<objective>
Build the artwork detail page with lightbox zoom, WhatsApp CTA, and contact form. Visitors see full artwork metadata, zoom into images, and reach out to buy.

Purpose: GAL-02 (metadata), GAL-03 (lightbox), GAL-04 (WhatsApp), GAL-05 (contact form) — the core conversion page where browsing turns into buyer inquiry.
Output: Detail page, lightbox viewer, contact form, WhatsApp button components.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ana-galeri/02-RESEARCH.md
@.planning/phases/02-ana-galeri/02-01-SUMMARY.md

@src/app/(main)/[locale]/layout.tsx
</context>

<interfaces>
<!-- From Plan 01 — query functions and server actions the executor will call -->

From src/lib/queries/gallery.ts (created in Plan 01):
```typescript
export async function getProductBySlug(slug: string): Promise<ProductWithDetails | null>
// Returns product with all images (ordered by sortOrder) and artist relation
// Product fields: id, slug, titleTr, titleEn, descriptionTr, descriptionEn, category, price, currency, year, mediumTr, mediumEn, dimensionsTr, dimensionsEn, isSold
// Artist fields: id, slug, nameTr, nameEn, whatsapp, email
// Images: { id, url, altTr, altEn, sortOrder }[]
```

From src/lib/actions/contact.ts (created in Plan 01):
```typescript
export const contactSchema = z.object({
  senderName: z.string().min(2).max(100),
  senderEmail: z.string().email(),
  body: z.string().min(10).max(2000),
  productSlug: z.string().optional(),
})
export type ContactFormState = { success: boolean; errors?: Record<string, string[]> }
export async function submitContact(data: z.infer<typeof contactSchema>): Promise<ContactFormState>
```

From src/lib/utils/whatsapp.ts (created in Plan 01):
```typescript
export function buildWhatsAppHref(phone: string, artworkTitle: string, pageUrl: string): string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lightbox viewer with next/image + WhatsApp button</name>
  <files>src/components/gallery/next-image-slide.tsx, src/components/gallery/lightbox-viewer.tsx, src/components/gallery/whatsapp-button.tsx</files>
  <action>
1. Create `src/components/gallery/next-image-slide.tsx` — Client Component:
   - Follow the yet-another-react-lightbox Next.js example pattern from RESEARCH.md
   - Import `Image` from `next/image`, `isImageFitCover`, `isImageSlide`, `useLightboxProps` from the library
   - Render `next/image` with `fill` prop inside a relative container sized to the slide dimensions
   - Return `undefined` for non-image slides (required by the library)

2. Create `src/components/gallery/lightbox-viewer.tsx` — Client Component:
   - **CRITICAL:** Import CSS files at the top:
     ```
     import 'yet-another-react-lightbox/styles.css'
     import 'yet-another-react-lightbox/plugins/captions.css'
     ```
   - Props: `{ slides: { src: string; alt: string; width?: number; height?: number; title?: string }[], thumbnails: { src: string; alt: string }[] }`
   - Uses `useState(-1)` for open/close index tracking
   - Renders thumbnail images as clickable buttons (onClick sets index)
   - Thumbnails: `next/image` in aspect-[3/4] containers, responsive grid (grid-cols-2 sm:grid-cols-3 lg:grid-cols-4)
   - First thumbnail larger: `col-span-2 row-span-2` for hero image effect
   - Lightbox uses Zoom + Captions plugins
   - Passes `render={{ slide: NextImageSlide }}` for CDN optimization

3. Create `src/components/gallery/whatsapp-button.tsx` — Server Component (no 'use client'):
   - Props: `{ phone: string, artworkTitle: string, pageUrl: string }`
   - Uses `buildWhatsAppHref()` from `@/lib/utils/whatsapp`
   - Renders `<a href={href} target="_blank" rel="noopener noreferrer">` with WhatsApp icon (inline SVG) and text "WhatsApp ile Sor"
   - Styled as a prominent green CTA button: `bg-[#25D366] text-white px-6 py-3 rounded-lg font-medium`
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>LightboxViewer opens on thumbnail click with zoom support. NextImageSlide renders via next/image. WhatsAppButton produces correct wa.me href. All type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Contact form + artwork detail page</name>
  <files>src/components/gallery/contact-form.tsx, src/app/(main)/[locale]/urun/[slug]/page.tsx</files>
  <action>
1. Create `src/components/gallery/contact-form.tsx` — Client Component:
   - Uses `react-hook-form` with `zodResolver` from `@hookform/resolvers/zod`
   - Import `contactSchema` from `@/lib/actions/contact` for client-side validation
   - Import `submitContact` from `@/lib/actions/contact` as the Server Action
   - Props: `{ productSlug: string }` (passed as hidden context)
   - Fields: senderName (text), senderEmail (email), body (textarea)
   - On submit: call `submitContact({ ...formData, productSlug })` directly (not useActionState — just async call)
   - Show loading state during submission (disable button, show spinner)
   - On success: show green success message "Mesajiniz gonderildi. En kisa surede donus yapacagiz."
   - On validation error: show field-level error messages in red below each input
   - Tailwind styling: inputs with border, focus:ring, labels above inputs, submit button full-width

2. Create `src/app/(main)/[locale]/urun/[slug]/page.tsx` — async Server Component:
   - Import `getProductBySlug` from `@/lib/queries/gallery`
   - Import `LightboxViewer`, `WhatsAppButton`, `ContactForm`
   - Props: `{ params: Promise<{ locale: string; slug: string }> }`
   - **CRITICAL:** `await params` (Next.js 16 async)
   - Fetch product: `const eser = await getProductBySlug(slug)` — if null, call `notFound()`
   - Layout structure (single column, clean art gallery aesthetic):
     a. **Image gallery** — LightboxViewer with slides from `eser.images`
     b. **Metadata section** — `<h1>` title (locale-aware), then a definition-list style grid:
        - Sanatci / Artist: artist name (link to artist if exists)
        - Teknik / Medium: mediumTr or mediumEn
        - Boyut / Dimensions: dimensionsTr or dimensionsEn
        - Yil / Year: year
        - Fiyat / Price: formatted price with currency (or "Fiyat icin iletisime gecin" if null)
     c. **Description** — descriptionTr or descriptionEn if exists
     d. **WhatsApp CTA** — WhatsAppButton with artist.whatsapp (only if artist has whatsapp number)
     e. **Contact form** — ContactForm with productSlug
   - Add `generateMetadata()` for SEO: title = artwork title, description = medium + dimensions
   - Locale helper: use `locale === 'tr'` to pick _tr vs _en fields throughout
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Detail page at /[locale]/urun/[slug] renders full artwork metadata, lightbox, WhatsApp CTA, and contact form. 404 for missing/hidden artworks. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete gallery experience</name>
  <action>
Human verifies the complete gallery experience built across Plans 02 and 03.

What was built: Gallery listing page with category filter at /tr/galeri, artwork detail page with lightbox zoom, WhatsApp CTA, and contact form at /tr/urun/[slug].

How to verify:
1. Start dev server: `pnpm dev`
2. Visit http://localhost:3000/tr/galeri — verify artwork grid renders with seed data
3. Click a category button — verify grid filters and URL updates with ?category=
4. Click "Tumunu Goster" — verify all artworks return
5. Click an artwork card — verify detail page at /tr/urun/[slug] loads
6. On detail page, verify: title, medium, dimensions, year, price are displayed
7. Click an artwork image — verify lightbox opens with zoom capability
8. Verify WhatsApp button shows with green styling
9. Fill contact form with valid data and submit — verify success message appears
10. Fill contact form with invalid email and submit — verify error message
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User confirms all 10 verification steps pass. Gallery listing and detail pages work as expected.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes
- `pnpm test -- --testPathPatterns=gallery` all green
- Visual: gallery grid renders, category filter works, detail page shows metadata, lightbox zooms, WhatsApp link opens, form submits
</verification>

<success_criteria>
- Detail page shows title, medium, dimensions, year, price for seeded artworks
- Lightbox opens on image click with zoom enabled
- WhatsApp button opens wa.me with pre-filled message containing artwork title
- Contact form validates and submits successfully, message saved to DB
- 404 returned for non-existent slugs
- All TypeScript compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ana-galeri/02-03-SUMMARY.md`
</output>
